import styled from 'styled-jss'

export const StyledButton = styled('button')({
  width: 'max-content',
  padding: '8px 24px',
  lineHeight: '1.5em',
  textAlign: 'center',
  fontSize: '16px',
  fontWeight: 700,
  border: 0,
  borderRadius: '24px',
  color: 'var(--lightest)',
  background: 'var(--primary)',
  cursor: 'pointer',
})
